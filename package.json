{
  "name": "roombridge",
  "version": "1.0.0",
  "private": true,
  "description": "International housing and roommate matcher for students across U.S. universities.",
  "scripts": {
    "dev": "concurrently \"npm run ai:dev\" \"npm run mcp:dev\" \"npm run server:dev\" \"npm run client:dev\"",
    "ai:dev": "./ai-service/venv/bin/uvicorn main:app --reload --port 8001 --app-dir ai-service",
    "server:dev": "npm --prefix server run dev",
    "mcp:dev": "npm --prefix mcp-server run dev",
    "review:dev": "cd listing-review-service && php artisan serve --port=8010",
    "review:test": "cd listing-review-service && composer test",
    "client:dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install && npm --prefix mcp-server install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
